plugins 目录下存放AOP插件工程，插件工程使用AspectJ方案

使用方（application、library）依赖plugins 目录下的aop module，
并在自身的module的build.gradle 中引用插件、配置

apply plugin: 'com.hujiang.android-aspectjx'

aspectjx {
    //关闭AspectJX功能
    enabled false
    //忽略所有的class文件及jar文件，相当于AspectJX不生效
    exclude '*'
}
