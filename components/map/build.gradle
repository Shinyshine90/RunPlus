apply from : '../../config/lib_base.gradle'

dependencies {
    implementation project(path: ':libs:amap')
    implementation "com.android.support.constraint:constraint-layout:${v_constraint_layout}"
}